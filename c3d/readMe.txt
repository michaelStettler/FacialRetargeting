Open pycharm (c3dToMaya project)

First conversion of all .c3d files to maya binary files (.mb)
run script: load_save_all.py

Open maya

In maya:
	import the c3d converted files (.mb)
	open scene (avatar) of interest 
	run scale.py (copy past script in command line in maya or alt+a in pycharm)
	run parentConstrain.py 